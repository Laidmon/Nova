[build]
  command = "npm run build"
  publish = "."
  node_version = "18"
  functions = "netlify/functions"

[[redirects]]
  force = true
  from = "/api/*"
  to = "/netlify/functions/:splat"
  status = 200
